# notifier.py: Contains functions for sending notifications (email and desktop notifications in your case).

# notifier.py will handle sending out notifications. For desktop notifications, we can use the plyer library, and for emails, we'll use smtplib.